    def character_score(self, _set_character_move):
        chara = Character()
        (chara.dx, chara.dy)
        return chara.dx, chara.dy

    def _set_character_move(self, direction, player_hit(color)):
        if direction and player_hit(red) += 1
        elif direction and player_hit(white) = 0
        return result
